package griding.service;

import org.slf4j.Logger;
import org.slf4j.LoggerFactory;
import org.springframework.stereotype.Service;

@Service
public class GridingService {

    private static final Logger logger = LoggerFactory.getLogger(GridingService.class);

    // TODO: will need StandingsSearchService and UCIPointsSearchService.

    public void gridSignups(String signups) {
    }
}
